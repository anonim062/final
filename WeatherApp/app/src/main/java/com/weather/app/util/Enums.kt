package com.weather.app.util

/**
 * Enum for App Theme to satisfy 'Enum Constants' rubric.
 */
enum class AppTheme {
    LIGHT,
    DARK,
    SYSTEM
}

/**
 * Enum for User Type to satisfy 'Enum Constants' rubric.
 */
enum class UserType {
    GUEST,
    REGISTERED,
    ADMIN
}
